# add travi yml file
language: java
dist: trusty
sudo: required

before_install:
- sudo apt-get install curl -y
- curl -o - https://get.docker.com | bash
script:
- ./gradlew clean packDeb
- chmod +x ./print.sh
- ./print.sh
