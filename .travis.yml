# add travi yml file
language: java
dist: trusty
sudo: required

before_install:
- sudo apt-get install curl -y
- docker run -d manikantanr/demo
- docker ps
- docker images
- ps aux

script:
- ./gradlew clean packDeb
- chmod +x ./print.sh
- ./print.sh
